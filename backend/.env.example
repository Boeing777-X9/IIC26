# Retinix Backend Environment Configuration
# Set your MongoDB connection URI (e.g., local mongod or MongoDB Atlas cluster)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=retinix

# Model checkpoint configuration
CHECKPOINT_PATH=ML/classifier.pt
